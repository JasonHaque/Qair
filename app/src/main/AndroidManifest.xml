<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jason.qair">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,ExtraText,GoogleAppIndexingWarning">
        <activity android:name=".LogActivity"></activity>
        <activity android:name=".ShowQuestion" />
        <activity android:name=".QuestionCreate" />
        <activity android:name=".ExamCreate" />
        <activity
            android:name=".Timeline"
            android:theme="@style/newTheme" />
        <activity android:name=".ApploginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".AccountPage" />
    </application>

</manifest>